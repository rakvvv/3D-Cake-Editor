KTX2 transcoder assets used by `KTX2Loader.setTranscoderPath('/assets/ktx2/')`.

Add the platform-appropriate `basis_transcoder.js` and `basis_transcoder.wasm` before runtime; both files are intentionally omitted from version control.
