export const environment = {
  production: true,
  apiBaseUrl: '/api',
  authorMode: false,
  debugInteractionNormals: false,
  endpoints: {
    saveScene: 'saveScene',
    scene: 'scene',
    decorations: 'decorations'
  }
};
