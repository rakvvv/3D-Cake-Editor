export const environment = {
  production: true,
  apiBaseUrl: 'https://api.cake-editor.local/api',
  authorMode: false,
  endpoints: {
    saveScene: 'saveScene',
    scene: 'scene',
    decorations: 'decorations'
  }
};
